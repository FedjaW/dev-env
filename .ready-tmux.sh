#!/usr/bin/env bash

# this is just an example file that shows that this script is executed when
# executing the "ready-tmux" script which is in my PATH environemnt

tmux neww -n foofoo # will create a new windows with name foofoo
